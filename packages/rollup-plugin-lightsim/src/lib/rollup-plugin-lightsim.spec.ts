import { rollupPluginLightsim } from './rollup-plugin-lightsim';

describe('rollupPluginLightsim', () => {
  it('should work', () => {
    expect(rollupPluginLightsim()).toEqual('rollup-plugin-lightsim');
  });
});
